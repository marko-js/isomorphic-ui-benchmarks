<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Isomorphic UI Benchmarks</title>
    </head>
    <body>
        <ul>
            <li for(benchmark in data.benchmarks)>
                <h1>${benchmark.name}</h1>
                <p>
                    <a href="/benchmark/${benchmark.name}">Run Client-side Benchmark</a>
                </p>
                <p>
                    View pages:
                    <ul>
                        <li for(bench in benchmark.benches)>
                            <a href="/${benchmark.name}/${bench.name}">${bench.name}</a>
                        </li>
                    </ul>
                </p>
            </li>
        </ul>
    </body>
</html>
