class {
    onInput() {
        this.input = null;
    }

    onMount() {
        window.onMount();
    }
}

<div.search-results>
    <div>
        <for(item in input.items)>
            <!-- Loop over the search results items: -->
            <app-search-results-item(item)/>
        </for>
    </div>
    <app-footer/>
</div>
