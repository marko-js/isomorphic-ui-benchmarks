class {
    onInput() {
        this.input = {};
    }

    onMount() {
        window.onMount();
    }
}

<div.search-results>
    <div>
        <for(item in (input.items || window.searchResultsData.items))>
            <!-- Loop over the search results items: -->
            <app-search-results-item(item)/>
        </for>
    </div>
    <app-footer/>
</div>
